__pycache__/
*.pyc
.venv/
.env

geometry/out/
geometry/*.stl
geometry/*.step
flow360/results/

# post/out/ holds the cached CFD digests (sweep_data.csv,
# linear_range.json, equilibrium_grid.{csv,md}) + reproducible plots.
# Keep these so `paper/build.sh` works without cloud access.  Volume
# meshes etc. never land here.

# ESP / OpenCSM intermediates
*.cpc
*.udc
*.tess
*.egads
*.jrnl
serveCSM.log

# Local editor / OS noise
.DS_Store
.idea/
.vscode/
geometry/out_high_htail/
geometry/out_gapped_high_htail/

# Local Claude session state
.claude/

# Heavy slice-output cache (re-downloadable via paper/figures/_retrieve_slices.py)
paper/figures/slices/

# LaTeX build artifacts
paper/*.aux
paper/*.log
paper/*.bbl
paper/*.blg
paper/*.out
